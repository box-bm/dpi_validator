name: dpi_validator
description: A DPI validator. This validate and return values of department and municipality by DPI.
version: 1.0.0
homepage: https://github.com/box-bm/dpi_validator
repository: https://github.com/box-bm/dpi_validator
issue_tracker: https://github.com/box-bm/dpi_validator/issues

environment:
  sdk: ">=2.18.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
